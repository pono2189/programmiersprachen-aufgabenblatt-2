#include "vec2.hpp"

class Circle{
    public:
        Circle(float radius, float surface, Vec2 location, Color col)
        : m_radius(radius), m_surface(surface), m_location(lacation), m_col(col) {}

    private:
        float m_radius {0.0f}; 
        float m_surface {0.0f};
        

}

